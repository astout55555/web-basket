{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Shared code is isomorphic (browser + Node) and sticks to web-standard
    // globals like atob/TextDecoder, which live in the DOM lib.
    "lib": ["ES2023", "DOM"]
  },
  "include": ["src"]
}
