# Web Basket

A request bin / webhook inspector: create a basket, point any HTTP request at its URL, and watch requests appear live on a dashboard.

> Work in progress — full README (architecture, local dev, Azure deployment) lands at the end of the build.
